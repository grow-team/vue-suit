// 左侧菜单收起折叠的key
export const vsSideMenuCollapse = 'SIDE_MENU_COLLAPSE'
// 存储打开tab页的key
export const vsVisitedSubmenu = 'VS_VISITED_SUBMENU'

// 左侧菜单自动处于收起状态的最大宽度
export const page = {
  maxScreenWidth: 900
}
